module: tests

flags: [
    '-x', 'c++',
    '-std=c++17',
    '-I./src'
]

units:
  facades:
    source: 'src/facades.h'
    target: 'src/facades_py_auto.cpp'

specs:
  'function@vectorFacadeFunction':
    args:
      a:
        facade:
          kind: vector
          length_arg: count
